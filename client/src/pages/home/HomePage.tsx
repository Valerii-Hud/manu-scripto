import MobileNavMenu from '../../common/ui/mobile/MobileNavMenu';

const HomePage = () => {
  return (
    <main className="home">
      <MobileNavMenu />
    </main>
  );
};

export default HomePage;
